# Dependencies
node_modules/
package-lock.json

# Local build output (reports are generated wherever you point --out)
out/
*.pdf

# Install metadata (per-machine)
.install-meta.json

# OS / editor noise
.DS_Store
Thumbs.db
